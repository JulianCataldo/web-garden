{
  "packages": ["app/*", "app/*/*", "configs"],
  "useNx": false,
  "version": "independent",
  "command": {
    "publish": {
      "message": "chore: publish release"
    }
  },
  "changelogPreset": {
    "name": "conventionalcommits",
    "issueUrlFormat": "{{host}}/{{owner}}/{{repository}}/issues/{{id}}"
  }
}
